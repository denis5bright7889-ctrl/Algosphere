import NewContentClient from './NewContentClient'

export const metadata = { title: 'New Content — Growth Engine' }
export const dynamic = 'force-dynamic'

export default function NewGrowthContentPage() {
  return <NewContentClient />
}
