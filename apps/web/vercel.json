{
  "framework": "nextjs",
  "installCommand": "npm install --prefer-dedupe",
  "buildCommand": "npm run build",
  "crons": [
    {
      "path": "/api/cron/growth-publish",
      "schedule": "*/2 * * * *"
    }
  ]
}
