fastapi==0.115.5
uvicorn[standard]==0.32.1
# Pinned <13 to satisfy supabase 2.4.6 -> realtime (websockets>=11,<13);
# uvicorn[standard] only needs >=10.4, FastAPI WS works fine on 12.x.
websockets==12.0
python-dotenv==1.0.1
supabase==2.4.6
httpx==0.27.0
apscheduler==3.10.4
numpy==2.1.3
pandas==2.2.3
loguru==0.7.2
pydantic==2.10.2
pydantic-settings==2.6.1
redis==5.2.0
python-multipart==0.0.18

# Credential vault — matches the Node lib/vault.ts AES-256-GCM byte-for-byte
cryptography==43.0.3

# Broker adapters (lazy-imported — only required if the matching API keys are set)
python-binance==1.0.29
pybit==5.8.0
python-okx==0.3.5
# MetaTrader5 is Windows-only and is installed on the MT5 host VPS, not Railway.
# Uncomment if you deploy this engine to a Windows runtime that drives MT5.
# MetaTrader5==5.0.45
